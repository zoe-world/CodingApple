let 회원들: { member1: string; member2: string } = {
  member1: "kim",
  member2: "park",
};

let 회원들1 = "park";
let 회원들2: number[] = [1, 2, 3];

// 예제문제
let userName: string = "이조은";
let userAge: number = 30;
let userAdd = "경기도 남양주시 진건오남로 735-17 101동 904호";

let singer: { song: string; name: string } = { song: "blue", name: "bigbang" };

// 타입지정
let project: { member: string[]; days: number; started: boolean } = {
  member: ["kim", "park"],
  days: 30,
  started: true,
};
